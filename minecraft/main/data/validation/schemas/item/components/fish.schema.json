{
	"$schema": "https://json-schema.org/draft/2020-12/schema",
	"$id": "components/fish.schema.json",
	"$defs": {},
	"type": "object",
	"additionalProperties": false,
	"properties": {
		"angle": {
			"description": "Direction the fish faces in its texture. Compass strings and degrees run clockwise from the top.",
			"oneOf": [
				{
					"type": "string",
					"enum": ["T", "TR", "R", "BR", "B", "BL", "L", "TL"]
				},
				{
					"type": "number",
					"minimum": 0,
					"exclusiveMaximum": 360
				}
			]
		},
		"size": {
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"averageLengthCm": {
					"description": "The fish's average collectible length in centimeters. Each catch's rolled length also determines its physically scaled shadow.",
					"type": "number",
					"exclusiveMinimum": 0
				},
				"lengthDeviationCm": {
					"description": "Optional override for the distance from the average that fewer than 1% of catches exceed. Defaults to one third of averageLengthCm.",
					"type": "number",
					"exclusiveMinimum": 0
				},
				"textureLengthPixels": {
					"description": "The fish's opaque length projected onto its facing direction and normalized to a 16-pixel item frame. Generate this value from texture.png and angle.",
					"type": "number",
					"exclusiveMinimum": 0
				}
			},
			"required": [
				"averageLengthCm",
				"textureLengthPixels"
			]
		},
		"catching": {
			"description": "Optional overrides for the stable catching behavior generated from the fish id and rarity.",
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"struggleSeconds": {
					"type": "number",
					"minimum": 0
				},
				"secondsAwayFromBobber": {
					"type": "number",
					"minimum": 0
				},
				"approachSeconds": {
					"description": "Seconds for a bounce approach from retreatDistance back to the bobber. The first arrival scales its duration to use this same speed.",
					"type": "number",
					"exclusiveMinimum": 0
				},
				"retreatSeconds": {
					"type": "number",
					"exclusiveMinimum": 0
				},
				"retreatDistance": {
					"description": "Blocks the fish backs away after touching the bobber. This is added to the size-based contact distance.",
					"type": "number",
					"exclusiveMinimum": 0
				},
				"averageCatchSeconds": {
					"description": "Target average seconds from the first bounce approach starting to the bite.",
					"type": "number",
					"minimum": 0
				}
			}
		},
		"tags": {
			"description": "Spawn conditions. Tags in the same category are alternatives (OR); different categories must all match (AND). Warm and cold only classify Overworld biomes.",
			"type": "array",
			"uniqueItems": true,
			"items": {
				"type": "string",
				"enum": [
					"warm",
					"cold",
					"river",
					"ocean",
					"day",
					"night",
					"deep",
					"high",
					"rainy",
					"thunderstorm",
					"snowy",
					"waxing",
					"waning",
					"fullmoon",
					"newmoon"
				]
			}
		},
		"furnaceResult": {
			"type": "string",
			"enum": [
				"white_meat",
				"red_meat",
				"crab_claw",
				"tentacle"
			]
		}
	},
	"required": [
		"angle",
		"size",
		"tags",
		"furnaceResult"
	]
}
