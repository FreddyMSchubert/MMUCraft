{
  "name": "mmu-resourcepack-generator",
  "version": "1.0.0",
  "private": true,
  "description": "TypeScript generator for the MMU MC Soc server resource pack.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "generate": "npm run build && node dist/cli.js"
  },
  "dependencies": {
    "sharp": "^0.34.1"
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "typescript": "^5.8.3"
  }
}
